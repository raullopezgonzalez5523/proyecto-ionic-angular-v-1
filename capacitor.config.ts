import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto-ionic-angular-v-1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
